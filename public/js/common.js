$(document).ready(function() {
    // enable tabs
    //$('.tabs').tab('show');

    $("body").tooltip({
        selector: "a[rel=twipsy]",
        placement: "bottom"
    });
    $('.dropdown-toggle').dropdown();
});